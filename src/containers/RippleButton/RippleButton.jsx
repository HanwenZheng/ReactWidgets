import React, { Component } from "react";
import styles from "./RippleButton.module.scss";

class RippleButton extends Component {
  render() {
    return (
      <div className={styles.RippleButton}>
        <div>RippleButton</div>
      </div>
    );
  }
}

export default RippleButton;
